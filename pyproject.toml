[project]
name = "gsn-print-service"
version = "0.1.0"
description = "GSN Print Service"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
